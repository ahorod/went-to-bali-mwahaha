var item_id = '<%= @item.id%>'
$("#item_in_cart_" + item_id).remove();

$("#cart_total").html('Your Total: <%= number_to_currency current_order.total_price %>');

$("#total_item").html('Cart | Total items: <%= cart_items %>');
